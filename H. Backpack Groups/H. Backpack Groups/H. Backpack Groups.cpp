#include <bits/stdc++.h>

typedef pair<int, int> pii;
using namespace std;



int main() {
	int v, n, t;
	cin >> v >> n >> t;

	vector<pii>stuff(n);
	for (int i = 0; i < n; i++) {
		int w, c, p;
		cin >> w >> c >> p;

	}
}